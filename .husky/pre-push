#!/usr/bin/env bash
# Pre-push: run eslint to catch issues before push (keep this in pre-push so commits stay fast)
echo "Running eslint before push..."
eslint --cache || {
  echo "eslint failed. Fix issues or run 'npm run lint:fix' then push again." >&2
  exit 1
}
