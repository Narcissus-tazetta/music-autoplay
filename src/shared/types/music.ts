export interface Music {
    url: string;
    title: string;
    thumbnail: string;
    channel?: string;
    channelName?: string;
    channelId?: string;
    requesterHash?: string;
    duration?: string;
    addedAt?: string;
    id?: string;
}

// named export only
